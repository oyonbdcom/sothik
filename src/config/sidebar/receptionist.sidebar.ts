import { dashboardMenu, passwordMenu } from "./common.sidebar";
import { MenuItemProps } from "./sidebar.types";

export const receptionistConfig: MenuItemProps[] = [
  dashboardMenu,
  passwordMenu,
];
